/**
 * Technical infrastructure layer for TruckFlow Manager.
 *
 * <p>This layer contains adapters, configuration concepts, technical exceptions, mapping contracts,
 * repository implementations and infrastructure services. It may depend on application outbound
 * ports and domain models, but domain and application must not depend on infrastructure.
 */
package it.gabriele.truckflow.infrastructure;
