/**
 * Technical adapters used to connect application ports to infrastructure mechanisms.
 *
 * <p>Adapters live outside the domain and application core. They can depend on application ports,
 * domain models and technical libraries, but the core must never depend on them.
 */
package it.gabriele.truckflow.infrastructure.adapter;
