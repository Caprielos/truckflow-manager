/**
 * Location repository prototype adapters for Punto 7E.
 *
 * <p>This package contains the first real repository prototype. It uses a simple file-backed
 * technical model to validate the repository + mapper pattern without introducing a database, JPA,
 * Spring Data or REST delivery layer.
 */
package it.gabriele.truckflow.infrastructure.repository.locations;
