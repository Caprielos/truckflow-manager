/**
 * In-memory repositories for ATP and temperature-controlled transport certificates.
 *
 * <p>These adapters are temporary persistence implementations used for tests, demos and the
 * pre-database phase of TruckFlow Manager.
 */
package it.gabriele.truckflow.infrastructure.memory.atp;
