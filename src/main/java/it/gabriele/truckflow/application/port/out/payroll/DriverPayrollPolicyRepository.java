package it.gabriele.truckflow.application.port.out.payroll;

import it.gabriele.truckflow.application.port.out.RepositoryPort;
import it.gabriele.truckflow.domain.payroll.DriverPayrollPolicy;

/** Repository port per DriverPayrollPolicy. L'implementazione sarà in infrastructure. */
public interface DriverPayrollPolicyRepository extends RepositoryPort<DriverPayrollPolicy> {}
