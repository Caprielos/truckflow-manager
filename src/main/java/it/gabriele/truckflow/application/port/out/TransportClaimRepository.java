package it.gabriele.truckflow.application.port.out;

import it.gabriele.truckflow.domain.claim.TransportClaim;

/**
 * Repository port per TransportClaim. L'implementazione sarà in infrastructure.
 */
public interface TransportClaimRepository extends RepositoryPort<TransportClaim> {
}
