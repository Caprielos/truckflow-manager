package it.gabriele.truckflow.application.port.out;

import it.gabriele.truckflow.domain.driverscheduling.DriverAbsence;

/** Repository port per DriverAbsence. */
public interface DriverAbsenceRepository extends RepositoryPort<DriverAbsence> {}
