/**
 * Application layer of TruckFlow Manager.
 *
 * <p>This layer orchestrates domain objects through use cases. It depends on the domain model and
 * exposes technology-neutral ports, commands, results, and application exceptions. It must not
 * depend on web controllers, databases, JPA, Spring annotations, or concrete infrastructure.
 */
package it.gabriele.truckflow.application;
