/**
 * Application service implementations of use case ports.
 *
 * <p>An application service coordinates commands, repositories, domain aggregates, and results. It
 * does not duplicate domain invariants and does not contain framework or persistence code.
 */
package it.gabriele.truckflow.application.usecase;
