package it.gabriele.truckflow.deadlineservice.domain;

/** Categoria funzionale di un elemento gestito dal servizio scadenze. */
public enum ManagedElementCategory {
  VEHICLE_DOCUMENT,
  DRIVER_DOCUMENT,
  VEHICLE_MAINTENANCE,
  TRAILER_COMPONENT,
  CARGO,
  WAREHOUSE,
  TRIP,
  SECURITY,
  TELEMATICS
}
