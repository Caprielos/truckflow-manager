package it.gabriele.truckflow.deadlineservice.rulepack;

/** Stato configurativo di una singola regola o di uno slot ancora da compilare. */
public enum DeadlineRuleSlotStatus {
  EMPTY_SLOT,
  ACTIVE,
  SUSPENDED
}
