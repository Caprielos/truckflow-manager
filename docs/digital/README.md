# Documentazione digitale

Questa cartella contiene la documentazione navigabile da browser.

Apri [`index.html`](index.html) per accedere alla mappa principale.

La vecchia cartella `digitalDocs/` è stata sostituita da `docs/digital/`.
